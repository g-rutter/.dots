# vim: set filetype=sh:

###########################
#  Environment variables  #
###########################

#export PATH=/home/theory/phrlaq/bin:$PATH
export LIBRARY_PATH=$HOME/lib:$HOME/lib64:$LIBRARY_PATH
export LD_LIBRARY_PATH=$HOME/lib:$HOME/lib64:$LD_LIBRARY_PATH
#export LD_PRELOAD=$HOME/lib/libxdo.so.2:$LD_PRELOAD
export PYTHONPATH=$PYTHONPATH:$HOME/lib/python2.6/site-packages/
export PKG_CONFIG_PATH=/home/theory/phrlaq/lib64/pkgconfig:/home/theory/phrlaq/lib/pkgconfig:/home/theory/phrlaq/share/pkgconfig:$PKG_CONFIG_PATH
export XDG_DATA_DIRS=$HOME/share:$XDG_DATA_DIRS

export CC=/usr/bin/gcc-4.9
export CXX=/usr/bin/g++-4.9

export INCLUDE=$HOME/include:$INCLUDE
export C_INCLUDE_PATH=$HOME/include
export CPLUS_INCLUDE_PATH=$HOME/include
export FPATH=$HOME/include:$FPATH
export CPATH=$HOME/include:$CPATH
export ACLOCAL_FLAGS="-I /home/theory/phrlaq/share/aclocal/"

#export INFODIR=$INFODIR:$HOME/share/info
#export INFOPATH=$INFOPATH:$HOME/share/info
#export MANPATH=$MANPATH:$HOME/share/man

export CPPFLAGS=-I$HOME/include:$CPPFLAGS
export LDFLAGS=-L$HOME/lib
export HOME2=/storage/molsim/phrlaq

export PREFIX=$HOME
export DISPLAY=:0.0

#for boost
export BOOST_BUILD_PATH=~/boost/tools/build/v2/
export CPATH=~/boost/:$CPATH
export LIBRARY_PATH=~/boost/stage/lib/:$LIBRARY_PATH

#CVS
export CVSROOT=:ext:phrlaq@godzilla:/home/cvs/phrlaq
export CVS_RSH=ssh

#############
#  Aliases  #
#############

alias mathematica="module load mathematica; mathematica"
alias ls="ls -x --color=auto --group-directories-first"
alias ll="ls -lhrt --color=auto --group-directories-first"
alias grip="grep -iI --color=auto"
alias grep="grep -I --color=auto"

alias rm="rm -I"             #-i: Ask you to confirm before deletions. Can be overridden at any time by using the -f option. -v: be verbose. Makes it say exactly what it is doing.

#############
#  Modules  #
#############

module load vmd
